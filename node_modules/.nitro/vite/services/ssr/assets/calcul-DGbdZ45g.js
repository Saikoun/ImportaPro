import { createFileRoute, lazyRouteComponent } from "@tanstack/react-router";
import { z } from "zod";
import { zodValidator } from "@tanstack/zod-adapter";
//#region src/routes/calcul.tsx
var $$splitComponentImporter = () => import("./calcul-CEgYJmOq.js");
var searchSchema = z.object({ id: z.string().optional().catch(void 0) });
var Route = createFileRoute("/calcul")({
	validateSearch: zodValidator(searchSchema),
	head: () => ({ meta: [{ title: "Calcul Import · ImportaPro" }, {
		name: "description",
		content: "Calculez instantanément le coût total d'un import : transport, produit, frais et marge."
	}] }),
	component: lazyRouteComponent($$splitComponentImporter, "component")
});
//#endregion
export { Route as t };
